fun Adult(x) return { type: "Adult", ...x }
const Child = Adult

const res = Adult({
    name: "John"
    children: [
        Child({ name: "Steve" })
        Child({ name: "Mary" })
    ]
})

print(res)